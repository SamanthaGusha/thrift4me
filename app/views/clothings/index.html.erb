<!--<h1>Welcome to Thrift4Me</h1>
<h2>Clothes</h2>

<div class="row justify-content-center">
  <div class="col-sm-6 my-3">
    <%#= form_tag root_path, method: :get, class: "d-flex" do %>-->
      <%#= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Type a keyword"
        %>
      <%#= submit_tag "Search", name: "", class: "btn btn-primary" %>
    <%# end %>
<!--  </div>
</div>


<div class="container m-4">
  <p><%# @clothings.each do |clothing| %></p>
  <div class="row d-flex justify-content-center">
    <div class="card my-5 text-center ">
      <%# if clothing.photo.attached? %>-->
        <%#= cl_image_tag(clothing.photo.key, width: 400, height: 300, crop: :fill) %>
      <%# else %>
        <%#= image_tag("https://replayjeans.co.za/wp-content/uploads/2023/04/AM4318_000_A0460_098_F-1.jpg", width: 400, height: 300, crop: :fill) %>
      <%# end %>
<!--
      <p><%#= "#{clothing.id}" %> <strong><%= "#{clothing.title}" %></strong></p>
      <p><%#= "#{clothing.description}" %></p>
      <p>R<%#=  "#{clothing.price}" %></p>
      <p>I am located at: <%#= clothing.latitude + clothing.longitude %>
      <img src="" alt="">
      <%# if user_signed_in? %>-->
<!--        <button class="btn">-->
        <%#= link_to "Show Details", clothing_path(clothing), style: "color: grey; text-decoration: none;opacity: 0.7;" %>
<!--        </button>-->
      <%# else %>
        <%#= link_to "Show Details", user_session_path, style: "color: grey; text-decoration: none;opacity: 0.7;" %>
      <%# end %>
<!--    </div>
  </div>
  <%# end %>-->
<!--</div>
-->

<h1>Welcome to Thrift4Me</h1>
<h2>Clothes</h2>

<div class="container my-3">
  <%= form_tag root_path, method: :get, class: "d-flex justify-content-center mb-4" do %>
    <%= text_field_tag :query,
          params[:query],
          class: "form-control mr-2",
          placeholder: "Type a keyword"
      %>
    <%= submit_tag "Search", name: nil, class: "btn btn-primary" %>
  <% end %>
</div>

<div class="container my-4">
  <div class="row">
    <% @clothings.each do |clothing| %>
      <div class="col-md-4 d-flex align-items-stretch">
        <div class="card my-3">

          <% if clothing.photo.attached? %>
            <%= cl_image_tag(clothing.photo.key, width: 400, height: 300, crop: :fit, class: 'img-fluid') %>
          <% else %>
            <%= image_tag("placeholder.jpg", class: 'img-fluid') %> <!-- Use a placeholder image -->
          <% end %>

          <div class="card-body">
            <h5 class="card-title"><%= clothing.title %></h5>
            <p class="card-text"><%= clothing.description %></p>
            <p class="card-text">R<%= clothing.price %></p>
            <p class="card-text">I am located at: <%= [clothing.latitude, clothing.longitude].compact.join(", ") %></p>
            <% if user_signed_in? %>
              <%= link_to "Show Details", clothing_path(clothing), class: "btn btn-secondary" %> <!-- Secondary button for a different style -->
            <% else %>
              <%= link_to "Show Details", new_user_session_path, class: "btn btn-secondary" %>
            <% end %>
          </div>

        </div>
      </div>
    <% end %>
  </div>
</div>
  